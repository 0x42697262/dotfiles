# Packages used by BSPWM

```sh
sxhkd network-manager-applet feh picom polybar fcitx5
```

# INFO

- I use `picom --experimental-backends` because I need to use blur effects, iirc.
- `fcitx5` i have this on startup

